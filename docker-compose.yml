version: '2'
services:
  db:
    image: postgres
  web:
    build: .
    command: bundle exec rackup -p 3000 -o 0.0.0.0 config.ru
    volumes:
      - .:/shorty
    ports:
      - "3000:3000"
    depends_on:
      - db
